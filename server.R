if (is.null(result_points) || length(result_points) == 0) {
  showNotification("Nenhuma parcela foi gerada para os talhões selecionados.", type = "error")
} else {
  # Salva o arquivo ou continua o processo normalmente
}
