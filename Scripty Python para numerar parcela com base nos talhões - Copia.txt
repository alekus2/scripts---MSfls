def calcular_maximo(projeto,talhao,parcela):
    index = projeto + talhao + parcela
    max_parcelas = {}
    #tem que contar o maximo do valor de parcelas do talhao concatenando de "ID_PROJETO" + "ID_TALHAO" + "nm_parcela".
    #para contar esse maximo ele tem q contar o numero de parcelas q esse talhao tem, mas sem ser só por nm parcela mas adicionando um contador que faz +1 toda vez que ele encntra uma parcela do mesmo talhao e do mesmo projeto com msm nm parcela tb.

    max_parcelas[talhao] = max(valores)
    return max_parcelas[talhao]

