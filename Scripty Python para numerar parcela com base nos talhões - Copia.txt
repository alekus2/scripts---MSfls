def contar_parcelas_por_index(idx):
    import arcpy
    tabela = "Shape_IFQ12_teste"
    where = f"\"Index\" = '{idx}'"
    with arcpy.da.SearchCursor(tabela, ["Index"], where_clause=where) as cursor:
        return sum(1 for _ in cursor)
