  all_pts <- do.call(rbind, result_points)

  # Numerar parcelas de 1 até N dentro de cada talhão (Index)
  all_pts <- all_pts %>%
    group_by(Index) %>%
    mutate(PARCELAS = row_number()) %>%
    ungroup()

  return(all_pts)
